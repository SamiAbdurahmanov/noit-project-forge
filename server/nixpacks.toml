# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["python311", "postgresql", "opencv"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"